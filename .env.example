# Client runtime environment variables

# Provide a valid PilotQA token (issued by your backend/dashboard)
# PILOTQA_AUTH_TOKEN=your_signed_token

# One of the following for RS256 public key resolution:
# 1) Path to PEM file within the project (recommended for air‑gapped envs)
# TOKEN_PUBLIC_KEY_PATH=PilotQA_AI/keys/public.pem

# 2) Inline PEM (use with caution)
# TOKEN_PUBLIC_KEY="-----BEGIN PUBLIC KEY-----\n...\n-----END PUBLIC KEY-----"

# 3) Remote token service that exposes /tokens and returns the current public key
# TOKEN_SERVICE_URL=https://your-token-service

